<ul>
  <% if ::Refinery::UserGroups::Admin::ItemsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.user_groups_admin_items_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_user_groups_admin_item_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::UserGroups::Admin::ItemsController.sortable? && ::Refinery::UserGroups::Item.any? %>
  <li>
    <%= link_to t('.reorder', :what => "Items"),
                 refinery.user_groups_admin_items_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Items"),
                 refinery.user_groups_admin_items_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
