<% content_for :body do %>
  <ul id="wine_groups">
    <% @wine_groups.each do |wine_group| %>
      <li>
        <%= link_to wine_group.name, refinery.wine_groups_wine_group_path(wine_group) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
